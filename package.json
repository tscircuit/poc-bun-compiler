{
  "name": "poc-bun-compiler",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "build:vfs": "make-vfs ./api --content-format=import-default --outfile=api-vfs.ts",
    "build:cli": "bun build ./server.ts --compile --outfile ./bin/tsci-compiler"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@semantic-release/commit-analyzer": "^11.1.0",
    "@semantic-release/git": "^10.0.1",
    "@tscircuit/builder": "^1.5.4",
    "@tscircuit/react-fiber": "^1.0.6",
    "make-vfs": "^1.0.8",
    "react": "^18.2.0",
    "redaxios": "^0.5.1",
    "rimraf": "^5.0.5",
    "semantic-release": "^22.0.7",
    "zod": "^3.22.4"
  }
}